import React from 'react';

import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface SearchJobsScreenProps {}

export function SearchJobsScreen(props: SearchJobsScreenProps) {
  return (
    <View>
      <Text>Welcome to search-jobs-screen!</Text>
    </View>
  );
}

export default SearchJobsScreen;
