import { config, themes } from "./tamagui.config";

// Shared theme can be overwritten here: use https://studio.tamagui.dev/ and past theme-builder.ts result here
export const themeConfig = config;

export { themes };
